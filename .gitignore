node_modules/
.env
*.local
.DS_Store

# Secrets / service-account keys — never commit
serviceAccountKey.json
googleCloudKey.json
.vercel

# Local Claude Code settings — machine-specific, not for the repo
.claude/settings.local.json

# Firebase CLI debug log
firebase-debug.log

# Removed legacy template — kept deleted and ignored
writing/activity/flip-template.html
