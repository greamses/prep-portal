{
  "name": "prep-portal",
  "private": true,
  "description": "Root manifest. The Vercel Edge middleware is dependency-free (verifies the Firebase session cookie with the built-in Web Crypto API), so no runtime deps are needed here.",
  "dependencies": {}
}
