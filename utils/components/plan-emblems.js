/* =========================================================================
   PLAN / SUBSCRIPTION EMBLEMS — re-export shim
   -------------------------------------------------------------------------
   The emblems now live with every other icon in `nav-icons.js` (single source
   of truth). This file is kept so existing importers keep working.
========================================================================= */
export {
  SVG_ROSE,
  SVG_CROWN,
  SVG_SHIELD,
  planEmblem,
  planTier,
} from "/utils/components/nav-icons.js";
